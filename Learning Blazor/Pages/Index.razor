@page "/"
@layout MainLayout

@using Microsoft.AspNetCore.Components.WebAssembly.Services

<HeadContent>
    <script src="_framework/blazor.webassembly.js"></script>
</HeadContent>

<div class="container">
    <h1>Blazor App</h1>

    <button class="btn btn-primary" @onclick="ExecutePythonFunction">Execute Python Function</button>
</div>

@code {
    private async Task ExecutePythonFunction()
    {
        var client = new HttpClient();
        var response = await client.PostAsync("https://unicodex-xwodoxs6eq-uc.a.run.app/", null);
        if (response.IsSuccessStatusCode)
        {
            var result = await response.Content.ReadAsStringAsync();
            // Do something with the result
        }
    }
}
